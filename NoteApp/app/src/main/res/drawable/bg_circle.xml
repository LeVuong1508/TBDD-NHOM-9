<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Màu nền -->
    <solid android:color="#FF4081" />

    <!-- Viền (nếu muốn) -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFF" />

    <!-- Kích thước tối thiểu (tùy chọn) -->
    <size
        android:width="48dp"
        android:height="48dp" />
</shape>
